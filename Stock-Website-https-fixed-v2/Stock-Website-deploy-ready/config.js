// Frontend deployment config
// Replace the empty string below with the REAL public backend HTTPS URL before sharing the site.
// Example: window.__API_ORIGIN__ = "https://api.your-domain.com";
window.__API_ORIGIN__ = window.__API_ORIGIN__ || "";
